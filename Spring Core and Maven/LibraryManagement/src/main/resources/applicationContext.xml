<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:context="http://www.springframework.org/schema/context"
       xmlns:aop="http://www.springframework.org/schema/aop"
       xsi:schemaLocation="
           http://www.springframework.org/schema/beans
           http://www.springframework.org/schema/beans/spring-beans.xsd
           http://www.springframework.org/schema/context
           http://www.springframework.org/schema/context/spring-context.xsd
           http://www.springframework.org/schema/aop
           http://www.springframework.org/schema/aop/spring-aop.xsd">

    <!--
        Exercise 6 — Component Scanning
        Picks up @Service, @Repository, @Component, @Aspect automatically
    -->
    <context:component-scan base-package="com.library"/>

    <!--
        Exercise 3 & 8 — Enable AspectJ auto-proxying for AOP support
    -->
    <aop:aspectj-autoproxy/>

    <!--
        Exercise 1 & 5 — Explicit XML Bean Definitions
    -->
    <bean id="bookRepository"
          class="com.library.repository.BookRepository"/>

    <!--
        Exercise 2, 5, 7 — Dependency Injection
        Constructor injection (Exercise 7) + Setter injection (Exercise 2, 5, 7)
    -->
    <bean id="bookService"
          class="com.library.service.BookService">
        <constructor-arg ref="bookRepository"/>
        <property name="bookRepository" ref="bookRepository"/>
    </bean>

    <!--
        Exercise 3 & 8 — Register LoggingAspect bean
    -->
    <bean id="loggingAspect"
          class="com.library.aspect.LoggingAspect"/>

</beans>
